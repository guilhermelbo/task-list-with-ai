spring.application.name=api

# Disable springdoc in tests
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Disable AI in tests to avoid needing real API keys or Ollama
ai.provider=disabled
